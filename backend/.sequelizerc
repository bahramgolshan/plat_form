// Even if the rest of your project is in ESM, this specific config file must use require() and module.exports.
const path = require('path')

module.exports = {
  config: path.resolve(__dirname, 'src/config/sequelize.config.js'),
  'models-path': path.resolve(__dirname, 'src/models'),
  'migrations-path': path.resolve(__dirname, 'migrations'),
  'seeders-path': path.resolve(__dirname, 'migrations/seeders'),
}
